version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
      - "5173:5173"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
      - ./playwright:/app/playwright
    environment:
      - PORT=3001
      - HEADLESS=true
    restart: unless-stopped
